#!/usr/bin/python

import glob, setuptools

setuptools.setup(
        name = 'sjconf',
        version = '@PACKAGE_VERSION@',
        py_modules = ['sjconf'],
        packages = ['sjconfparts'],
    )
